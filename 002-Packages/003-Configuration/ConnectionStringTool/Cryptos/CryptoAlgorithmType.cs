namespace ConnectionStringTool.Cryptos
{
    public enum HashAlgorithmType { MD5, SHA1, SHA256, SHA384, SHA512 }
    public enum SymmetricAlgorithmType { DES, TripleDES, AES, RC2, Rijndael }
    public enum AsymmetricAlgorithmType { RSA }
}
